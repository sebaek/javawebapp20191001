tomcat(JAVA ee)
-원하는 디렉토리에 적절한 파일
-servlet이 요청(request)을 받고 응답(response)을 함
 --HttpServlet 클래스를 상속 받아 만들면 됨

-HTML을 쉽게 작성하기 위해서 JSP 사용
 -- JSP도 Servlet

-JSP 문법
 -- 디렉티브 <%@ %>  // 페이지의 부가정보
 -- 스크립트릿 <% %> // Java 코드
 -- 표현식expression <%= %> // out.print()의 파라미터
 -- 선언부declration <%! %> // 필드나 메소드만듬

- 요청을 다른 곳으로 보내는 방법
- response.sendRedirect()
  -- 클라이언트에게 다른 곳으로 요청을 보내라고 명령
  -- 새로운 request가 만들어짐
- RequestDispatcher.forward(request, response)
  -- JSP로 request을 전송하고 response하도록 함







